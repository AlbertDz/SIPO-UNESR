<nav class="nav color-b-blue-2">
    <section class="nav-ul-section font-12">
        {{> search }}
        <ul class="nav-ul">
            <li class="nav-li container c-point">
                <a href="/profile" class="no-link color-white">
                    <span class="icon">
                        <i class="fas fa-home"></i>
                    </span>
                    <p class="nav-li-text font-b uppercase">Inicio</p>
                </a>
            </li>
            {{#each user.enlaces}}
            <li class="nav-li container c-point">
                <a href="{{url}}" class="no-link color-white">
                    <span class="icon">
                        <i class="{{icon}}"></i>
                    </span>
                    <p class="nav-li-text font-b uppercase">{{concepto}}</p>
                </a>
            </li>
            {{/each}}
        </ul>
    </section>
</nav>